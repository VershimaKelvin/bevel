on:
#these events will trigger this workflow
#this work is triggered on pull request and on pushes into these branches

  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - main
      - master
      - develop
name: "Build & Release"
jobs:
  build:
    name: Run Test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.5'
          channel: 'stable'

      - run: flutter pub get
        name: analyze code
      - run: flutter analyze
        name: format lib folder
      - run: flutter format lib
      - run: get difference between files
###      - run: flutter build apk --debug --split-per-abi
#      - run: flutter test
#      - run: flutter packages pub run build_runner build --delete-conflicting-outputs
##      - run: flutter build apk --flavor development -t lib/main.dart
###      - name: Push to Releases
###        uses: ncipollo/release-action@v1
####        with:
####          artifacts: "build/app/outputs/apk/debug/*,build/ios/iphoneos/app.ipa"
####          tag: v1.0.${{ github.run_number }}
####          token: ${{ secrets.TOKEN }}






# push to master, main, develop
# pull request on main master